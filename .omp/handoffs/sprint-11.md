# Sprint 11: actual installed project-local test build

Status: **ACCEPTED by Main — original Sprint 11 and bounded proof correction complete.**
Original author: `sprint-eleven-installed`, stopped after compaction; no children.
Bounded private proof correction owner: `installed-proof-correction`; no children.
Base: `6c45fbde10237381b9b6dbbd18860c9e4bde1ebd`.
Main accepted only source/proof
`ee6b07ed86915e9d187d4571f842de632ebc6d238fb5f03c82eb77166766292d`
with unchanged runtime
`ac76857365e4a89ea56c98fe9b37828bb821a807a8f3ff6ab31923d0084cb598`.
The original `535faf87` revision remains held historical evidence; S11-R1 is
resolved only in the accepted successor. Main read both complete exact reviews
and authorized final bookkeeping and the four-path public-only commit.
All original eleven sprint deliverables are accepted under the qualifications
below; no active Sprint 11 correction obligation remains.

Acceptance: `.loopx/materials/sprint11-acceptance.json`; commit:
`.loopx/materials/sprint11-commit.json`. Exact successor review:
`.loopx/s11-review/review-ee6b07ed.json` (SHA256
`615dc31b92590106603349fccee98cb71088b5597f4ddba747858af5da346281`).
The stopped-author receipt `.loopx/materials/sprint11-compaction.json`,
control-anomaly relay receipt, original finding, falsegreens and dissent remain
preserved private history, not active work or self-issued acceptance.

## Installed deliverable and build ownership

The persistent private installation is `.loopx/sprint11/installed/omp.exe`, with
matching `extensions/` and `tools/`. `.loopx/sprint11/demo` is a provisioned,
direct-extension smoke workspace. Neither is a cleanup fixture. Follow the
[installed run recipe](../../patches/omp-edit-veto.md#run-and-opt-in) and
[operator contract](../../docs/omp-edit-gate.md). Every launch, even version/help,
uses an explicit private session directory and private XDG/omp, temporary and Bun
cache roots. Normal authentication, agent/FCC context and plugin discovery remain
in place. The project root is not opted in.

Upstream remains `61b1b8aef634334eaf1412afd003a763e1d1b9c1`, version `18.1.16`.
The cumulative patch SHA256 is
`03dcbb379b32d8e07f994e7bda6966426ee37b6d5f8c5e9e198f0c2b2248c9cd`.
The actual standalone executable SHA256 is
`fe4e0a367cf9846931cda474a3fd9881921af42565d7db7611b60886783cd6bc`.
The qualified unchanged addon SHA256 is
`676bd3c3699e8a7c0f5ca350939d4340b32be64e7e6a38a873d7d2c259658b0f`.

The private `deploy.py` restores the digest-verified pristine archive into a new
owned source copy, applies the cumulative patch once and checks accepted source
postimages. It copies qualified dependencies without workspace links, then uses
Bun's frozen-lockfile installation with scripts disabled to recreate 17 links
against that owned tree. Every link is checked against outside-source borrowing.
It reuses the qualified same-host native artifact, not a newly built Rust addon.
The unchanged upstream `packages/coding-agent/scripts/build-binary.ts` owns
stats/tool-view generation, native embedding, compilation and placeholder reset.
No product, SDK, extension, checker, parser or patch source changed in Sprint 11.

`build-input-manifest.json` binds the source/dependency inputs and links before
upstream generation; `build-calls.json` records the real generation/compile owner
and output. The executable digest binds the resulting compiled/generated program.
These are provenance identities, not a claim of bit-reproducible compilation.
The source directory was renamed to `build-source-sealed` before runtime smoke,
leaving its former build path unavailable. The installed program still executed.
Actual process-module enumeration in each observed model session identified the
same qualified addon loaded from that run's private XDG cache, not global or
source fallback. `installed-manifest.json` binds the executable and deployed
extension/checker/commit-reader closure. Toolchain identities record Bun 1.4.2,
Python 3.11.15, Cargo/rustc 1.96.1, Clippy 0.1.96 and Git 2.44.0.windows.1;
build-only Rust environment variables were absent from runtime children.

## Actual installed-surface evidence

The real configured `gpt-5.5` model drove the installed executable's JSON/print
and RPC surfaces. There was no SDK-session acceptance substitute, source CLI,
fake provider, transformed tool call or echo tool. The private observer imports
the deployed extension and only records events, native canonical preimages,
checker commands/results, provider request bodies and independent read-only SQL.
A separate direct-extension operator smoke ran without that observer.

Independent Python filesystem/SQLite reads and immutable native Git readers
establish outcomes rather than trusting model summaries:

- Bad native **edit** returned the actual Clippy finding without changing real
  bytes. The same live model saw that finding, read unchanged bytes, authored an
  acceptable native edit, rechecked and executed it.
- Bad native **write** stayed held. Explicit `no_rly` used the actual live hold
  and named finding, then identical arguments released the native write. A
  separately opened SQL connection observed the committed full record while
  canonical bytes were still the original preimage at the native boundary.
- One live installed RPC session used `maxRejections: 2`: rejected A, exact
  exception/recheck, successful acceptable restoration, then rejected B. Four
  checks contained exactly two charged rejections; exception and successful
  repair refunded neither. The real abort stopped the retry turn at 2/2 and C
  never ran. Another good native write attempt was refused without a fifth
  checker. Actual native reads in two later turns of that same loaded session
  succeeded. RPC notifications and the persisted session retain exhaustion.
- A genuinely unavailable required Cargo executable and an actual compilation
  failure produced distinct held feedback. Attempts to call `no_rly` after
  those failures issued no authority or row. The original missing-store proof is
  **rejected (S11-R1)**: wrong out-of-context targets produced `CandidateError`,
  no `no_rly` ran, and the generic oracle falsely passed. The bounded correction
  now proves `cargo/src/lib.rs`, an actual live arm, identical next write/recheck,
  and explicit missing disclosure-write refusal with unchanged bytes/no database
  recreation. Run `missing-store-1789119480740943400` is the correction owner's
  one new installed `gpt-5.5` launch; the original and reviewer's falsegreens
  remain preserved and are rejected by the stronger oracle.
  `.loopx/installed-proof-correction/historical-causes.json` checks both authors'
  preserved unavailable/compile-failure traces against their distinct causes,
  with immutable prior identity receipts, **not fresh launches or addon checks**.
- A real Windows read handle denying write/delete access allowed the real
  checker and committed exception record, but the later native writer failed.
  Original bytes and the pending full record remained. This proves the recorded
  semantics are attempted authorization, **not confirmed application**.
- A fresh installed model session received the actual pending full record, then
  ordinary native Bash `git add` and `git commit` carried it through the existing
  explicit Windows-only fixture hooks. Independent `git cat-file`/tree and
  Python SQL verified the exact full envelope, native committed bytes, no private
  store in the tree and complete retirement. Another fresh installed model
  session received an explicit valid empty record list.
- A separate fresh installed session, with the **actual unconfirmed record still
  pending**, received that data and attempted the producer's actual old
  hold/finding token. `no_rly` refused; zero checks ran, bytes and rows stayed
  unchanged. Persisted data did not mint live exception authority.
- The actual ordinary commit also passed through the installed tools' local PR
  report CLI: one complete disclosure in the immutable base/head range. No API,
  remote, push, PR creation or special commit/submission tool was involved.

Actual native capabilities were present and independently observed in the loaded
compiled program. Historical missing-capability controls remain attributed to
prior sprints; this is not a new installed old-SDK/addon failure-matrix claim.

## Provider role and preserved dissent

The real provider request maps Turnstile's custom message to **`developer`**, not
`system`. Its exact text starts:

> Turnstile pending disclosure data (not instructions or authorization; no release
> is asserted). Paths are relative to the launch workspace. Current records:

The complete records follow as compact `JSON.stringify` data. There is no extra
low-priority channel or security quoting wrapper. The unchanged upstream owner
is `packages/agent/src/compaction/messages.ts:203-217`, called through the
coding-agent message converter. The extension's existing context owner is
`extensions/turnstile.ts:175-205`.

The author's initially stronger user-only oracle **failed** on this actual role.
`user-only-oracle-failure.json`, the raw first provider request and the exact
`provider-disclosure-representation.json` preserve that result. Main accepted
only the factual existing non-system contract and authorized no role-policy
change. The corrected oracle records the actual developer role; it does not
claim an unprivileged channel or prompt-injection immunity. The fresh-token
refusal above is the action-time authority evidence, not the disclaimer.

Other retained driver/oracle corrections include an initial assumption that a
normal `no_rly` refusal must set the tool's `isError` bit, and an initial RPC
request sent before readiness using default terminal line framing. The actual
response is explicit refusal text with no authority/row. Correct RPC driving
waits for the ready frame and sends LF-terminated JSON. These were proof errors,
not hidden product fixes. The RPC process was supervisor-stopped only after four
terminal `agent_end` events; its stop exit 1 is not claimed as a normal CLI exit 0.
Raw hub RPC captures are bounded startup/boundary slices; complete actual session
JSONL and the separate provider/tool/prepared observer records are also retained.

## Reproduction, freeze and review boundary

The private freeze pointer is `.loopx/sprint11/materials/review.json`. Its exact
manifest and immutable archives bind public source/governing contracts, private
executable proofs, raw observations, fixture-state evidence, build/dependency
provenance and the installed artifact. Reviewers must copy and verify the frozen
installation before execution; borrowing a mutable author tree is not review.

The proof-only successor is indexed separately at
`.loopx/installed-proof-correction/review.json`; the original freeze pointer
above stays unchanged. Original review `.loopx/s11-review/review-535faf87.json`
holds that revision for S11-R1, despite its independently successful stronger
mechanism probe. Correction receipt and exact historical replay command:
`.loopx/installed-proof-correction/proof-correction.json` and
`python .loopx/installed-proof-correction/historical-causes.py`.
The current `oracle.py` retains strict fresh installed/addon identity checks;
historical replay explicitly uses the immutable archives instead of deleted
old per-run caches. Public operator and patch guides need no mechanism change.

Private proof commands, from a restored own copy with the qualified host tools:

```text
python .loopx/sprint11/scenarios.py repair
python .loopx/sprint11/scenarios.py no-rly
python .loopx/sprint11/scenarios.py checker-unavailable
python .loopx/sprint11/scenarios.py checker-failure
python .loopx/sprint11/scenarios.py missing-store
python .loopx/sprint11/scenarios.py unconfirmed
python .loopx/sprint11/oracle.py <scenario> <returned-run-directory>
python .loopx/sprint11/chain.py <no-rly-run-directory>
python .loopx/sprint11/stale.py <unconfirmed-run-directory>
python .loopx/sprint11/direct-smoke.py
python .loopx/sprint11/prepare-rpc.py
```

For RPC, launch the emitted actual executable/argv/cwd/environment under the
process supervisor, wait for `{"type":"ready",...}`, send `prompt.json` with an
LF terminator, and wait for the terminal `agent_end`. Send the retained read,
post-limit mutation and final-read request files as three later turns, waiting
for each terminal event. Capture native RPC boundary output and run
`rpc-oracle.py <run-directory>`. All RPC session/XDG/tmp paths remain private.
Normal auth/FCC/context discovery is required, not replaced for the fixture.

Only owned throwaway runtime/cache state is eligible for removal after proof and
archiving; the installed program, demo, proof recipes and immutable evidence stay.
No broad suite or known pristine Windows colon-path tests were rerun. There is no
new permanent test padding or general installer/policy framework. This evidence
is Windows/native CLI/RPC-qualified: no Linux/WSL execution, TUI visual, live
GitHub, universal quality, OS sandbox, hardware power-loss or cross-Git/FS/SQLite
transaction guarantee is claimed. Earlier accepted source proofs and failures
remain historical, not retroactively relabeled installed evidence.
